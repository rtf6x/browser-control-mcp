/** Default WebSocket port for browser extensions. Health probe uses {@link DEFAULT_MCP_HTTP_PORT}. */
export const DEFAULT_WS_PORT = 18789;

/** Default HTTP MCP endpoint port (Streamable HTTP + /health). */
export const DEFAULT_MCP_HTTP_PORT = 18790;
